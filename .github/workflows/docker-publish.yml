name: Docker Build and Publish

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag for the Docker image'
        required: true
      environment:
        description: 'Environment to deploy to'
        required: true
        type: choice
        options: [production, staging]
      skip_tests:
        type: boolean
        default: false

permissions:
  contents: read
  packages: write

jobs:
  route:
    uses: bitcoinuniverseio/.github-private/.github/workflows/route.yml@develop
    secrets: inherit
    with:
      plan: '{"push_to_registry":"docker"}'

  test:
    if: ${{ !inputs.skip_tests }}
    uses: ./.github/workflows/setup-python.yml
    with:
      python-version: '3.10'

  push_to_registry:
    needs: [route, test]
    if: ${{ always() && (inputs.skip_tests || success()) }}
    runs-on: ${{ fromJSON(needs.route.outputs.targets).push_to_registry }}
    environment: ${{ inputs.environment }}
    steps:
      - uses: runs-on/action@v2
      - name: Check out the repo
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@d7f5e7f509e45cec5c76c4d5afdd7de93d0b3df5 # v4
      
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@c299e40c65443455700f0fdfc63efafe5b349051 # v5
        with:
          images: ghcr.io/bitcoinuniverseio/btc-stamps-indexer
          tags: |
            type=raw,value=${{ inputs.tag }}
            type=sha,prefix=,suffix=,format=short
      
      - name: Build and push
        uses: docker/build-push-action@f9f3042f7e2789586610d6e8b85c8f03e5195baf # v7
        with:
          context: ./indexer
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
      
      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: 'ghcr.io/bitcoinuniverseio/btc-stamps-indexer:${{ inputs.tag }}'
          format: 'table'
          exit-code: '1'
          severity: 'CRITICAL'
          skip-files: 'app/bootstrap/srcbackground.csv,/usr/src/app/files/**'

      - name: Generate SBOM
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0
        with:
          format: spdx-json
          artifact-name: sbom.spdx.json
